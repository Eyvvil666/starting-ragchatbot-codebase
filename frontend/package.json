{
  "name": "ragchatbot-frontend",
  "version": "1.0.0",
  "description": "Frontend for the RAG Course Materials Chatbot",
  "scripts": {
    "format": "prettier --write \"**/*.{js,css,html}\"",
    "format:check": "prettier --check \"**/*.{js,css,html}\"",
    "lint": "eslint .",
    "quality": "npm run format:check && npm run lint"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.3.3"
  }
}
